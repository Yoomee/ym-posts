.modal-header
  %a.close{:"data-dismiss" => "modal"} x
.modal-body
  -if @post.has_video?
    %iframe{:width => 560, :height => 315, :src => @post.video_embed_url, :frameborder => 0, :allowfullscreen => true}
  -else
    =image_for(@post, "560x>")